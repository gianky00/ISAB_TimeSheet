[build]
  command = "pip install poetry && poetry config virtualenvs.create false && poetry install --with dev --no-interaction && mkdocs build"
  publish = "site"

[build.environment]
  PYTHON_VERSION = "3.12"
